<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
    <a href="{{ path('post_index') }}">post list</a>
    <a href="{{ path('comment_index') }}">comment list</a>
    <a href="{{ path('post_index') }}">back to list</a>
    <a href="{{ path('post_index') }}">back to list</a>

</html>
